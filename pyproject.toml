[project]
name = "superroo-medical"
version = "0.1.0"
description = "Local 3D Slicer imaging pipeline and clinical decision-support scaffold for SuperRoo"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
